//Write function that reverses a list, preferably in place.


function reorder(array){
    var ind=array.length-1;
    var narr=[];
    for(i=0;i<array.length;i++){var b=ind-i;
    let c=array[i];
narr[b]=c;}
return narr;
}
let array=[2,3,4,5,6,7,8,9];
alert(reorder(array));
